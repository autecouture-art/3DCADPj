# テスト手順書

## デバッグとテスト結果

### ✅ ビルドテスト
- **結果**: 成功
- **TypeScript型チェック**: エラーなし
- **ビルドサイズ**: 1,180.10 kB (gzip: 331.61 kB)

### 🔧 修正した問題

#### 1. DimensionPanel.tsx - 型安全性の改善
- **問題**: `selectedEntity`の型キャストが不足
- **修正**: `LineEntity`, `RectangleEntity`, `CircleEntity`への明示的なキャスト追加
- **影響**: 寸法追加時の型エラーを防止

#### 2. ExtrudeDialog.tsx - 押し出しロジックの改善
- **問題**:
  - 負の深さが正しく動作しない
  - スケーリングが一貫していない
  - 形状の中心位置が考慮されていない
- **修正**:
  - 常に正の深さを使用し、負方向の場合は回転で対応
  - スケール係数を統一 (0.02)
  - 矩形・円を中心に配置

## 手動テスト手順

### 1. スケッチモードの基本機能

#### 1.1 平面選択
1. モデラーモードを開く
2. 平面選択ダイアログが表示されることを確認
3. 「正面」「上面」「右側面」の3つのボタンがあることを確認
4. いずれかの平面をクリック
5. **期待結果**: スケッチキャンバスが表示される

#### 1.2 線分描画
1. 「線分」ツールを選択
2. キャンバス上でクリック（始点）
3. 別の場所でクリック（終点）
4. **期待結果**: 緑色の線分が描画される

#### 1.3 矩形描画
1. 「矩形」ツールを選択
2. キャンバス上でドラッグ
3. **期待結果**: 緑色の矩形が描画される

#### 1.4 円描画
1. 「円」ツールを選択
2. 中心点をクリック
3. ドラッグして半径を決定
4. **期待結果**: 緑色の円が描画される

### 2. エンティティ選択機能

1. 「選択」ツールを選択
2. 描画済みの図形をクリック
3. **期待結果**:
   - 選択された図形が黄色でハイライト
   - 端点・角点・中心点が黄色の点で表示
   - 右側に寸法パネルが表示

### 3. 寸法拘束機能

#### 3.1 寸法の追加
1. 図形を選択
2. 寸法パネルの「＋ 追加」ボタンをクリック
3. **期待結果**:
   - 線分: `L1`（長さ）
   - 円: `R1`（半径）
   - 矩形: `W1`（幅）

#### 3.2 寸法の編集
1. 寸法の数値をクリック
2. 新しい値を入力（例: 100）
3. Enterキーまたは✓ボタンをクリック
4. **期待結果**: 図形が新しい寸法に従って変形

#### 3.3 寸法の削除
1. 寸法の🗑ボタンをクリック
2. **期待結果**: 寸法が削除される

### 4. 幾何拘束機能

#### 4.1 水平拘束
1. 線分を選択
2. 左側の幾何拘束パネルで「水平」ボタンをクリック
3. **期待結果**:
   - 線分が水平になる
   - 拘束一覧に「━ 水平」が表示

#### 4.2 垂直拘束
1. 線分を選択
2. 「垂直」ボタンをクリック
3. **期待結果**:
   - 線分が垂直になる
   - 拘束一覧に「┃ 垂直」が表示

#### 4.3 拘束の削除
1. 拘束の✕ボタンをクリック
2. **期待結果**: 拘束が削除される

### 5. 押し出しボス/ベース機能

#### 5.1 矩形の押し出し
1. 矩形を描画
2. ツールバーの「押し出し」ボタンをクリック
3. 押し出しダイアログが表示される
4. 深さを10mm に設定
5. 方向を「⬆ 正方向」に設定
6. 「✓ 押し出し」ボタンをクリック
7. **期待結果**:
   - 3Dビューに戻る
   - 直方体が表示される

#### 5.2 円の押し出し（円柱）
1. 新しいスケッチで円を描画
2. 「押し出し」ボタンをクリック
3. 深さを20mm に設定
4. 「✓ 押し出し」ボタンをクリック
5. **期待結果**: 円柱が表示される

#### 5.3 負方向への押し出し
1. 図形を描画
2. 「押し出し」ボタンをクリック
3. 方向を「⬇ 負方向」に設定
4. 「✓ 押し出し」ボタンをクリック
5. **期待結果**: 反対方向に押し出される

### 6. 統合テスト（完全なワークフロー）

1. **準備**: モデラーモードで正面平面を選択
2. **描画**: 矩形ツールで100×100の矩形を描画
3. **選択**: 選択ツールで矩形をクリック
4. **寸法**: 寸法パネルで「＋ 追加」をクリック
5. **編集**: 寸法値を50に変更
6. **確認**: 矩形が50×50に変形することを確認
7. **押し出し**: 「押し出し」ボタンをクリック
8. **設定**: 深さ30mm、正方向を設定
9. **実行**: 「✓ 押し出し」をクリック
10. **確認**: 50×50×30の直方体が3Dビューに表示される

### 7. エラーハンドリングテスト

#### 7.1 空のスケッチで押し出し
1. エンティティを描画せずに「押し出し」ボタンをクリック
2. **期待結果**: 「スケッチにエンティティがありません」のアラート

#### 7.2 不正な寸法値
1. 寸法を編集で0または負の値を入力
2. **期待結果**: 値が保存されず、元の値に戻る

## 既知の制限事項

1. **線分からの押し出し**: 3本以上の線分が必要で、閉じたパスを自動認識しない
2. **複数図形の同時押し出し**: 現在は最初の図形のみ押し出される
3. **幾何拘束**: 水平・垂直のみ実装（平行・直角は未実装）

## パフォーマンステスト

- エンティティ数: 50個まで快適に動作
- 寸法数: エンティティあたり10個まで推奨
- 押し出し深さ: 0.1mm ～ 50mm が推奨範囲

## ブラウザ互換性

- ✅ Chrome/Edge (推奨)
- ✅ Firefox
- ✅ Safari (iOS含む)
- ⚠️ モバイルブラウザ: ビューワーモードのみ

## 次のステップ

今後の改善案:
1. 平行・直角などの追加の幾何拘束
2. 複数図形の選択と一括押し出し
3. 押し出しカット機能
4. 回転ボス/ベース機能
5. フィーチャーツリー（履歴管理）
6. スケッチの編集・再利用
